import { WeatherDirective } from './weather.directive';

describe('WeatherDirective', () => {
  it('should create an instance', () => {
    const directive = new WeatherDirective();
    expect(directive).toBeTruthy();
  });
});
